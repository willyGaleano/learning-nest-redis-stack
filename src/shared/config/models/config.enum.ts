export enum NodeEnv {
  DEVELOPMENT = 'development',
  PRODUCTION = 'production',
}

export enum Environment {
  LOCAL = 'LOCAL',
  QA = 'QA',
  STG = 'STG',
  PRD = 'PRD',
}

export enum LogLevel {
  TRACE = 'trace',
  DEBUG = 'debug',
  INFO = 'info',
  WARN = 'warn',
  ERROR = 'error',
  FATAL = 'fatal',
}
